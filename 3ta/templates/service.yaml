apiVersion: v1
kind: Service
metadata:
  name: {{ include "3ta.fullname" . }}
  labels:
    {{- include "3ta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "3ta.selectorLabels" . | nindent 4 }}
